# ============================================================
# LightDM GTK Greeter — Cyberpunk Neon
# Deployed to: /etc/lightdm/lightdm-gtk-greeter.conf (as root)
# ============================================================

[greeter]
# Use our cyberpunk wallpaper as background
background          = /home/generic/.config/wallpaper/wallpaper.png

theme-name          = Adwaita-dark
icon-theme-name     = Papirus-Dark
cursor-theme-name   = Adwaita
cursor-theme-size   = 24

font-name           = JetBrains Mono 11

hide-user-image     = false

# Clock panel indicator
indicators          = ~host;~spacer;~clock;~spacer;~session;~power
clock-format        = %a %b %d  %H:%M

position            = 50%,center
